function [W] = L1GraphKNNonSample(Data,sampleIdx,K)
% L1 Graph based on KNN over sampled data
% input:
%   Data -- row: samples, columns: features;
%   sampleIdx -- 1 < sampleids < size_of_row(Data)
%   K    -- K nearest neighbor;
% output:
%   
%



end